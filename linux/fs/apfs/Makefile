# SPDX-License-Identifier: GPL-2.0-only
#
# Makefile for the Linux APFS filesystem integration scaffolding.
#
# The directory started as empty for the initial Phase 0 kickoff step.
# Follow-up commits add buildable APFS scaffolding incrementally.

obj-$(CONFIG_APFS_FS) += apfs.o
apfs-y := super.o
